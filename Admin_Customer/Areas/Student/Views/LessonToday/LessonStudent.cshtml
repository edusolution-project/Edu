
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutStudent.cshtml";
    var model = ViewBag.Model as BaseCustomerMVC.Models.DefaultModel;
    var _lessonID = ViewBag.LessonID as string;
    var _classID = ViewBag.ClassID as string;
}
<div class="container-fluid">
    <!-- Begin Page Content -->
    <div id="exam-student">
    </div>
    <!-- End of Main Content -->
    <input type="hidden" name="ExamID" id="ExamID" value="" />
</div>
@section Scripts{
    @*<script src="~/js/student-exam-new.js"></script>
    <script>
    var config = {
        id: "exam-student",
        url: {
            load: '@Url.Action("GetLesson", "MyCourse")',
            current: '@Url.Action("GetCurrentExam","Example")',
            start: '@Url.Action("Create","Example")',
            chose: '@Url.Action("CreateDetails","Example")',
            end: '@Url.Action("CompleteExam","Example")'
        },
        lesson_id: '@_lessonID',
        class_id: '@_classID'
    }*@
    @*// thieeu cai dem => roi submit*@
    @*var urlStart = '@Url.Action("Create","Example")';
    var urlChose = '@Url.Action("CreateDetails","Example")';
    var urlComplete = '@Url.Action("CompleteExam","Example")';
    var urlCurrentExam = '@Url.Action("GetCurrentExam","Example")';
    var C_lessonID = '@_lessonID';
    var C_classID = '@_classID';*@

    @*</script>*@
    @*<script>
        $(document).ready(function () {
            load.lesson('@_lessonID','@_classID','@Url.Action("GetLesson", "MyCourse")');
        });
    </script>
    <script src="~/js/student-exam.js"></script>*@
    <script>
        function openNav() {
            $('#lessonContainer').addClass('col-md-10');
            $("#quizNavigator").show();
        }

        function closeNav() {
            $('#lessonContainer').removeClass('col-md-10');
            $("#quizNavigator").hide();
        }

    </script>
    <script src="~/js/student-exam-new.js"></script>
    <script>
        var config = {
            id: "exam-student",
            url: {
                load: '@Url.Action("GetLesson", "MyCourse")',
                current: '@Url.Action("GetCurrentExam","Example")',
                start: '@Url.Action("Create","Example")',
                chosen: '@Url.Action("CreateDetails","Example")',
                end: '@Url.Action("CompleteExam","Example")'
            },
            lesson_id: '@_lessonID',
            class_id: '@_classID'
    }
    var widget = new ExamStudent.onReady(config);
    </script>
}